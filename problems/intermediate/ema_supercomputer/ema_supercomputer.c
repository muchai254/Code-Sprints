#include <stdio.h>

/**
 * Finds the maximum product of areas of two non-overlapping pluses.
 *
 * @param n the number of rows in the grid
 * @param m the number of columns in the grid
 * @param grid the 2D grid array of characters
 * @return maximum product of areas of two pluses
*/

int twoPluses(char** grid, int n, int m) {
    // Write your code here
    return 0;
}
